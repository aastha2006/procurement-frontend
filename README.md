
  # Procurement Process Workflow

  This is a code bundle for Procurement Process Workflow. The original project is available at https://www.figma.com/design/xpATmYcKk8Sr77pE92MVo4/Procurement-Process-Workflow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  